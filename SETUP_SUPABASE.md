# שלב 1 — הקמת ה-Backend (Supabase) | ~10 דקות

זה ה"מוח" המשותף: מסד נתונים אחד שכל המשתמשים מתחברים אליו.
עושים את זה **פעם אחת**. בסוף תהיה לך כתובת + מפתח שאדביק לאפליקציה בשלב 2.

---

## א. יצירת חשבון ופרויקט
1. כנס ל‑**https://supabase.com** → **Start your project** → הירשם (אפשר עם Google/GitHub). חינם.
2. לחץ **New project**.
   - **Name:** drone-mgmt (או כל שם).
   - **Database Password:** בחר סיסמה חזקה ו**שמור אותה** (לא נדרשת לשימוש יומיומי).
   - **Region:** בחר את הקרוב ביותר (למשל Frankfurt).
3. לחץ **Create new project** והמתן ~דקה עד שמוכן.

## ב. כיבוי אימות אימייל (כדי שמשתמש+סיסמה יעבדו מיד)
1. בתפריט הצד: **Authentication** → **Sign In / Providers** (או **Providers**).
2. ודא ש‑**Email** מופעל.
3. בתוך הגדרות Email כבה את **Confirm email** (אישור אימייל) — כדי שלא ידרשו אימיילים אמיתיים.
   (אם אינך מוצא — תחת **Authentication → Settings** חפש "Confirm email" וכבה.)

## ג. יצירת הטבלאות וההרשאות
1. בתפריט הצד: **SQL Editor** → **New query**.
2. פתח את הקובץ **`supabase_schema.sql`** ששלחתי, **העתק את כולו**, הדבק בחלון, ולחץ **Run**.
3. אמור להופיע "Success". (זה יצר את כל הטבלאות, ההרשאות, היומן, והקטלוג.)

## ד. העתקת הכתובת והמפתח (אלה מה שצריך לי לשלב 2)
1. בתפריט הצד: **Project Settings** (גלגל שיניים) → **API**.
2. העתק שני ערכים:
   - **Project URL** — נראה כמו `https://abcd1234.supabase.co`
   - **anon public** key — מחרוזת ארוכה שמתחילה ב‑`eyJ...`
   > ה‑anon key מיועד להיות חשוף בדפדפן — ההרשאות (RLS) הן שמגנות. **אל תשתף** את ה‑service_role key.

## ה. יצירת המנהל הראשון
1. כנס לאפליקציה (שלב 2) והירשם עם שם משתמש **admin** + סיסמה שתבחר.
2. חזור ל‑Supabase → **SQL Editor** → הרץ:
   ```sql
   update public.profiles set role='admin' where username='admin';
   ```
3. מעכשיו admin הוא מנהל, ויכול לקבוע מהאפליקציה מי עורך ומי צופה.

---

## מה הלאה (שלב 2 — אני בונה)
שלח לי את שני הערכים מסעיף ד' (**Project URL** ו‑**anon key**), ואני:
1. אחבר את האפליקציה ל‑Supabase (מסך התחברות, תפקידים, סנכרון נתונים, מסך ניהול משתמשים, מסך יומן).
2. אתן לך `index.html` מעודכן להעלאה ל‑Tiiny.
3. נבדוק יחד משני טלפונים שהמידע משותף ושהיומן מתעדכן.

> שים לב: ה‑anon key הוא ציבורי (מוגן ע"י ההרשאות), אז זה תקין שאדביק אותו בקובץ. עדיין — שתף אותו רק איתי כאן.

## תפקידים (תזכורת)
- **מנהל** — הכל + ניהול משתמשים + יומן.
- **עורך (מחתים)** — החתמה/החזרה/עריכה/אפסון. לא נוגע בקטלוג/משתמשים.
- **צופה** — קריאה בלבד + ייצוא.

כל משתמש חדש שנרשם מתחיל כ**צופה**, והמנהל מקדם אותו לעורך לפי הצורך.
