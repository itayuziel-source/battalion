# תוכנית גרסת Backend — מערכת ניהול רחפנים (ארמו"ן גדוד 28)

מסמך תכנון. מטרה: להפוך את האפליקציה המקומית (קובץ HTML יחיד) למערכת **רב‑משתמשים**
עם **נתונים משותפים**, **התחברות לפי תפקיד**, ו**יומן מי‑עשה‑מה**.

---

## 1. תמונה גדולה

```
   משתמשים (טלפון/מחשב)
        │  לינק (Tiiny/Netlify)
        ▼
   ┌─────────────────┐      קריאה/כתיבה מאובטחת      ┌──────────────────────┐
   │  Frontend (HTML)│  ───────────────────────────▶ │  Supabase (Backend)  │
   │  אותו ממשק קיים │  ◀───────────────────────────  │  Postgres + Auth +   │
   └─────────────────┘                                │  הרשאות (RLS) + API  │
                                                      └──────────────────────┘
```

- **ה‑Frontend נשאר אותו ממשק** שכבר בנינו — רק מחליפים את שמירת הנתונים מ‑localStorage
  לקריאה/כתיבה מול ה‑backend.
- **Supabase** = שירות ענן שנותן בחבילה אחת: מסד נתונים (Postgres), התחברות משתמשים (Auth),
  **הרשאות אמיתיות בצד השרת** (Row‑Level Security), ו‑API אוטומטי. יש **שכבה חינמית** שמספיקה לנו.
- אפשר עדיין לארח את ה‑HTML על Tiiny — הוא רק "ידבר" עם Supabase.

> למה לא נשארים בקובץ סטטי? כי נתונים משותפים + יומן מרכזי + הרשאות אמיתיות **חייבים** מסד נתונים מרכזי.

---

## 2. תפקידים והרשאות (נאכפות בצד השרת, לא ניתנות לעקיפה)

| תפקיד | מה רואה | מה רשאי לעשות |
|-------|----------|----------------|
| **מנהל** (אדמין) | הכל | הכל: קטלוג, ערכות, פלוגות, ניהול משתמשים, איפוס, צפייה ביומן המלא |
| **מחתים** (מנפיק) | הכל | החתמה, החזרה, עריכה/העברה, שינוי סטטוס ערכה (אפסון). **לא** עורך קטלוג/משתמשים |
| **צופה** | קריאה בלבד | צפייה בהחתמות/היסטוריה/פילוח, ייצוא. בלי שינויים |

**שאלה פתוחה — "סיווג":** אם הכוונה שמפקד פלוגה יראה **רק את הפלוגה שלו** — נוסיף שדה "פלוגה" למשתמש
והרשאות יסננו אוטומטית לפי זה. צריך להחליט (ראה סעיף 8).

---

## 3. מודל הנתונים (טבלאות ב‑Postgres)

- `profiles` — משתמשים: שם, תפקיד (מנהל/מחתים/צופה), פלוגה (אופציונלי), פעיל/מושבת.
- `drone_types` — סוגי רחפן.
- `catalog_items` — קטלוג פריטים לכל סוג (כולל דגל "ראשי").
- `kits` — ערכות: סוג, מספר, סטטוס (זמין/מושאל/באפסון), הערות.
- `companies` — פלוגות (א/ב/ג/מסייעת).
- `soldiers` — חיילים: שם, מ"א, דרגה, פלוגה.
- `signouts` — החתמות: חייל, ערכה, מחתים, פלוגה, תאריכים, סטטוס (פעיל/הוחזר), פריטים שנמסרו, פריטים שהוחזרו, חוסרים, הערות.
- `audit_log` — יומן פעולות (סעיף 4).

---

## 4. יומן פעולות ("מי עשה מה")

כל פעולה שמשנה נתונים כותבת שורה אוטומטית:

| שדה | דוגמה |
|-----|--------|
| מתי | 19/06/2026 22:14 |
| מי | סמל אבי לוי (מחתים) |
| פעולה | החתמה / החזרה / עריכה / שינוי קטלוג / שינוי סטטוס ערכה / כניסה |
| על מה | AVATA ערכה #3, חייל דוד כהן |
| פרטים | "הועבר מדוד כהן למאור לוי" |

- היומן **מרכזי** — רואים בו פעולות של כולם, מכל המכשירים.
- מסך "יומן" למנהל + ייצוא ל‑Excel.
- מיושם ב‑DB עצמו (triggers) כדי שאי אפשר "לדלג" עליו מהצד של הדפדפן.

---

## 5. אבטחת מידע — שער הכרחי ⚠️

המערכת מחזיקה **שמות חיילים, מ"א, וציוד** — מידע שמחייב התייחסות אבטמ"ש.

- **לפני הקמה צריך אישור** להחזקת המידע אצל ספק ענן חיצוני (Supabase מתארח בארה"ב/אירופה).
- אם ענן חיצוני **לא** מאושר — חלופה: **Supabase בהתקנה עצמית (self‑hosted)** על שרת פנימי של היחידה,
  או שרת קטן פנימי. אותה ארכיטקטורה, רק "הבית" שונה.
- הרשאות נאכפות בשרת (RLS), חיבור מוצפן (HTTPS), והמפתח הציבורי בלבד מוטמע ב‑HTML (מאובטח ע"י RLS).

**המלצה:** לברר את שאלת הענן מול אבטמ"ש **לפני** השלבים הטכניים.

---

## 6. שלבי ביצוע

| שלב | מה | תוצר |
|-----|-----|------|
| 0 | החלטת אבטחת מידע (ענן מול פנימי) | אישור/כיוון |
| 1 | הקמת Supabase + סכמת טבלאות + זריעת הקטלוג | מסד נתונים מוכן |
| 2 | התחברות + 3 תפקידים (מסך login) | כניסה מאובטחת |
| 3 | חיבור הממשק הקיים ל‑backend (במקום localStorage) | נתונים משותפים |
| 4 | יומן פעולות + מסך צפייה/ייצוא | מעקב מי‑עשה‑מה |
| 5 | מסך ניהול משתמשים (מנהל יוצר משתמשים ותפקידים) | ניהול עצמאי |
| 6 | העלאה (Tiiny/Netlify) ובדיקת רב‑מכשירים | מערכת חיה |

- ה‑**ייצוא ל‑Excel והגיבוי** שכבר קיימים — נשמרים.
- מיגרציה: גיבוי ה‑JSON הקיים מהאפליקציה המקומית ייובא פעם אחת לטבלאות.

---

## 7. עלות

- **Supabase – שכבה חינמית** מספיקה (מסד עד 500MB, עד עשרות אלפי משתמשים פעילים בחודש).
- אירוח ה‑Frontend: Tiiny/Netlify — חינם.
- בהתקנה עצמית: עלות השרת הפנימי בלבד.

---

## 8. החלטות שצריך ממך לפני שמתחילים לבנות

1. **אבטחת מידע:** ענן חיצוני (Supabase) מאושר, או חובה פנימי/Self‑hosted?
2. **שיטת התחברות:** אימייל+סיסמה, או שם‑משתמש+סיסמה (נוח יותר ליחידה בלי אימיילים)?
3. **"סיווג":** האם תפקיד בלבד (מנהל/מחתים/צופה), או גם **הגבלה לפי פלוגה** (כל מפקד רואה את שלו)?
4. **מי מקבל משתמש:** רק צוות מנפיק/אפסנאות (כמה אנשים), או גם מפקדי פלוגות לצפייה?
5. **כמה משתמשים בערך** צפויים במערכת?

לאחר שתענה — אעדכן את התוכנית ונתחיל משלב 1.
